#include <stdio.h>

#define MAX_SIZE 20

int arr[MAX_SIZE];
int x;

int main() {
  for(int i = 0; i < MAX_SIZE; i++) {
    scanf("%d", &arr[i]);
  }

  for(int i = 0; i < MAX_SIZE - 1; i++) {
  }

  printf("%d", x);

  return 0;
}